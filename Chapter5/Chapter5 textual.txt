5.1 程序在逻辑上不需要但是语法需要时用空语句。

5.2 花括号括起来叫块

5.4 a) 应该在while外面定义iterator并初始化
    b) status作用域在while块中，在while外面声明变量

5.7 a) 第二行没分号
    b) 加{}
    c) 第二个if换成else if
    d) 变成==

5.8 上面有嵌套if，只有一个else叫悬垂else
    c++匹配最近if

5.13 a) 没有break
     b) ix得在switch外声明
     c) 把,改成:
     d) 正确

5.15 a) ix该在循环外定义
     b) ix后面无;
     c) sz也++，死循环

5.18 a) 没有{}
     b) 声明得在外部
     c) 声明得在外部

5.22 while(get_size() <= 0){/*code*/}